spring:
  profiles:
    active: dev
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false
  cloud:
    loadbalancer:
      retry:
        enabled: false
management:
  endpoints:
    web:
      exposure:
        include: '*'
